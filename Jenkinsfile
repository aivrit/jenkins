pipeline {
    agent {
        node {
            label ''
            customWorkspace '/var/cmp/infrastructure'
        }
    }
    stages {
        stage('test') {
            steps {
                sh 'pwd'
            }
        }
    }
}
